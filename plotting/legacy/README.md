# Legacy

Earlier iterations of the plotting/visualisation code, superseded by the
scripts in `plotting/`. Kept for provenance and reproducibility of prior
pipeline versions, not for re-use — these scripts are not maintained and are
not guaranteed to run as-is against the current data layout or dependencies.

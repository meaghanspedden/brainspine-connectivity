# Legacy

Earlier iterations of the analysis pipeline, superseded by the scripts in
`source/`. Kept for provenance and reproducibility of prior pipeline versions,
not for re-use — these scripts are not maintained and are not guaranteed to run
as-is against the current data layout or dependencies.
